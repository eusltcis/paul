
<?php


$server = "localhost";
$user = "root";
$pass = "";
$db = "students1";


$conn = new mysqli($server, $user, $pass, $db);

if($conn->connect_error){
    die("Connection failed: " . $conn->connect_error);
} else {
    echo "Connected successfully";
}


?>





<?php 

require_once 'dbconnection.php';

if (isset($_POST['login'])) {
    $username = $_POST['username'];
    $password = $_POST['password'];

    $sql = "SELECT * FROM students WHERE username = '$username' AND password  = '$password' ";

    $result = $conn->query($sql);

    if ($result->num_rows > 0) {

            session_start() ;
            $_SESSION['username'] = $username;



           
            echo "Login successful";
            header("Location: welcome.php");

        } else {
            echo "Login failed";
        }
    
}



?>


<form action="" method="post">
        <label for="name">username</label>
        <input type="text" name="username" id="name" required>
        <br>
         <label for="password">Password</label>
        <input type="password" name="password" id="password" required>
        <br>
        <button type="submit" name="login">Login</button>
    </form>








<?php 

require_once 'dbconnection.php';


if (isset($_POST['register'])) {
    $username = $_POST['username'];
    $password = $_POST['password'];

    $sql = "INSERT INTO students (username, password) VALUES ('$username', '$password')";

    if ($conn->query($sql) === TRUE) {
        echo "<br>New record created successfully";
    } else {
        echo "Error: " . $sql . "<br>" . $conn->error;
    }
}



?>



<h1>Rgistration </h1>
    <form action="" method="post">
        <label for="name">username</label>
        <input type="text" name="username" id="name" required>
        <br>
         <label for="password">Password</label>
        <input type="password" name="password" id="password" required>
        <br>
        <button type="submit" name="register">Register</button>
        <a href="login.php">Login</a>

    </form>
















<head>
   
   
   
    <title>Matrix Multiplication</title>
</head>
<body>


    <h1>Matrix Multiplication</h1>
    <form method="POST">
        <label for="size">Enter the size of the matrices (n x n):</label>
        <input type="number" name="size" id="size" required>
        <input type="submit" value="Create Matrices">
    </form>





  
  






    <?php
	
	
	 
	
		
	
    if ($_SERVER['REQUEST_METHOD'] == 'POST' && isset($_POST['size'])) {
           $size = (int)$_POST['size'];
                 echo '<form method="POST">';
				 
                 echo '<h2>Enter Matrix A : </h2>';
				  
				 
				 
				 
				 
        for ($i = 0; $i < $size; $i++) {
            echo '<input type="text" name="a[' . $i . ']" placeholder="Row ' . ($i + 1) . '" required>';
        }
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
        
        echo '<h2>Enter Matrix B:</h2>';
        for ($i = 0; $i < $size; $i++) {
            echo '<input type="text" name="b[' . $i . ']" placeholder="Row ' . ($i + 1) . '" required>';
        }
		
		
		
                  echo '<input type="hidden" name="size" value="' . $size . '">';
                  echo '<input type="submit" value="Multiply Matrices">';
                  echo '</form>';
    }
	
	
	
	
	
	
	

    if ($_SERVER['REQUEST_METHOD'] == 'POST' && isset($_POST['a']) && isset($_POST['b'])) {
        $size = (int)$_POST['size'];
        $a = array_map('trim', $_POST['a']);
        $b = array_map('trim', $_POST['b']);
        
       
        $matrixA = array();
        $matrixB = array();
        
        for ($i = 0; $i < $size; $i++) {
            $matrixA[$i] = array_map('intval', explode(' ', $a[$i]));
            $matrixB[$i] = array_map('intval', explode(' ', $b[$i]));
        }
        

        $result = array_fill(0, $size, array_fill(0, $size, 0));

        
       /* for ($i = 0; $i < $size; $i++) {
            for ($j = 0; $j < $size; $j++) {
                for ($k = 0; $k < $size; $k++) {
                    $result[$i][$j] += $matrixA[$i][$k] * $matrixB[$k][$j];
                }
            }
        }
		*/
		
		
		
		
		
		
		
		
		
		

        
        echo '<h2>Result of Matrix A X Matrix B:</h2>';
        echo '<table border="1">';
        for ($i = 0; $i < $size; $i++) {
            echo '<tr>';
            for ($j = 0; $j < $size; $j++) {
                echo '<td>' . $result[$i][$j] . '</td>';
            }
            echo '</tr>';
        }
        echo '</table>';
    }
    ?>
</body>
</html>









<html>
<head>


</head>


<body>
 <h1>Matrix Multiplication</h1>
    <form method="POST">
        <label for="size">Enter the size of the matrices (n x n):</label>
        <input type="number" name="size" id="size" required>
        <input type="submit" value="Create Matrices">
    </form>
	
	







</body>

</html>









